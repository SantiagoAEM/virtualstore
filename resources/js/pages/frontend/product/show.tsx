
import { Product } from "@/types";

export default function Show({ product, variations }: { product: Product, variations: number }) {
  console.log(product, variations);
  return (
    <div>Show 4:54:00</div>
  )
}